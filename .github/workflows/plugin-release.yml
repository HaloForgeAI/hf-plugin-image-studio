name: Plugin Release

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "GitHub release tag to create or update"
        required: true
      release_name:
        description: "Optional release title"
        required: false
      source:
        description: "Catalog source label"
        required: true
        default: "official"

permissions:
  contents: write

jobs:
  package-plugin:
    name: Package plugin
    runs-on: windows-latest

    env:
      HF_PLUGIN_SIGNING_PRIVATE_KEY: ${{ secrets.HF_PLUGIN_SIGNING_PRIVATE_KEY }}
      HF_PLUGIN_SIGNING_KEY_ID: ${{ secrets.HF_PLUGIN_SIGNING_KEY_ID }}
      HF_ADMIN_TOKEN: ${{ secrets.HF_ADMIN_TOKEN }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Rust target
        shell: pwsh
        run: rustup target add x86_64-pc-windows-msvc

      - name: Checkout plugin tooling
        shell: pwsh
        run: |
          git clone https://github.com/HaloForgeAI/haloforge-plugin-api.git "$env:RUNNER_TEMP/hf-plugin-api"
          git -C "$env:RUNNER_TEMP/hf-plugin-api" checkout 286c208f4a8f0c27e8c5b55d0f961b75e6c85306

      - name: Build plugin tooling
        shell: pwsh
        run: |
          npm install --prefix "$env:RUNNER_TEMP/hf-plugin-api/sdk"
          npm run build --prefix "$env:RUNNER_TEMP/hf-plugin-api/sdk"
          npm install --prefix "$env:RUNNER_TEMP/hf-plugin-api/pack"
          npm run build --prefix "$env:RUNNER_TEMP/hf-plugin-api/pack"

      - name: Use local plugin tooling
        shell: pwsh
        run: |
          $script = @'
          const fs = require("fs");

          const pkg = JSON.parse(fs.readFileSync("package.json", "utf8"));
          pkg.dependencies ??= {};
          pkg.devDependencies ??= {};
          pkg.dependencies["@haloforge/plugin-sdk"] = `file:${process.env.RUNNER_TEMP.replace(/\\/g, "/")}/hf-plugin-api/sdk`;
          pkg.devDependencies["@haloforge/plugin-pack"] = `file:${process.env.RUNNER_TEMP.replace(/\\/g, "/")}/hf-plugin-api/pack`;
          fs.writeFileSync("package.json", `${JSON.stringify(pkg, null, 2)}\n`);
          '@
          Set-Content -Path .hf-use-local-tooling.cjs -Value $script
          node .hf-use-local-tooling.cjs
          Remove-Item .hf-use-local-tooling.cjs
          npm install --package-lock-only --ignore-scripts

      - name: Validate release inputs
        shell: pwsh
        run: |
          $tag = "${{ inputs.release_tag }}".Trim()
          $title = "${{ inputs.release_name }}".Trim()

          if ([string]::IsNullOrWhiteSpace($tag)) {
            throw "release_tag is required."
          }
          if ($tag -match "System\.Collections\.Hashtable" -or $title -match "System\.Collections\.Hashtable") {
            throw 'Workflow inputs were expanded incorrectly. In PowerShell, use $($release.tag) instead of "$release.tag" when dispatching this workflow.'
          }
          if ($tag -notmatch '^v\d+\.\d+\.\d+(?:[-+][0-9A-Za-z.-]+)?$') {
            throw "release_tag '$tag' must look like v1.2.3 or v1.2.3-rc.1."
          }
          if ([string]::IsNullOrWhiteSpace($title)) {
            $title = "Image Studio $tag"
          }

          "RELEASE_TAG=$tag" | Out-File -FilePath $env:GITHUB_ENV -Append
          "RELEASE_TITLE=$title" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Package .hfpkg
        shell: pwsh
        run: |
          node "$env:RUNNER_TEMP/hf-plugin-api/pack/dist/cli.js" pack . --release --target x86_64-pc-windows-msvc --out dist/plugin-release
          $pkg = Get-ChildItem dist/plugin-release -Filter *.hfpkg | Select-Object -First 1
          if (-not $pkg) { throw "No .hfpkg artifact was created." }
          $sha = Get-ChildItem dist/plugin-release -Filter *.hfpkg.sha256 | Select-Object -First 1
          if (-not $sha) { throw "No .hfpkg.sha256 file was created." }
          "PKG_PATH=$($pkg.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append
          "PKG_NAME=$($pkg.Name)" | Out-File -FilePath $env:GITHUB_ENV -Append
          "SHA_PATH=$($sha.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Create or update GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          function Invoke-ReleaseUpload {
            param(
              [Parameter(Mandatory = $true)][string]$Tag,
              [Parameter(Mandatory = $true)][string]$Path
            )

            if (-not (Test-Path $Path)) {
              throw "Release asset not found: $Path"
            }

            $assetName = Split-Path -Leaf $Path
            for ($attempt = 1; $attempt -le 3; $attempt++) {
              & gh release upload $Tag $Path --clobber
              if ($LASTEXITCODE -eq 0) {
                return
              }
              if ($attempt -eq 3) {
                throw "Failed to upload $assetName to release $Tag after $attempt attempts."
              }
              Start-Sleep -Seconds (2 * $attempt)
              & gh release view $Tag --json tagName,url *> $null
            }
          }

          & gh release view $env:RELEASE_TAG --json tagName,url *> $null
          if ($LASTEXITCODE -ne 0) {
            & gh release create $env:RELEASE_TAG --title $env:RELEASE_TITLE --notes "Automated HaloForge plugin release"
            if ($LASTEXITCODE -ne 0) {
              throw "Failed to create release $env:RELEASE_TAG."
            }
            Start-Sleep -Seconds 2
          } else {
            & gh release edit $env:RELEASE_TAG --title $env:RELEASE_TITLE --notes "Automated HaloForge plugin release"
            if ($LASTEXITCODE -ne 0) {
              throw "Failed to update release $env:RELEASE_TAG."
            }
          }

          Invoke-ReleaseUpload -Tag $env:RELEASE_TAG -Path $env:PKG_PATH
          Invoke-ReleaseUpload -Tag $env:RELEASE_TAG -Path $env:SHA_PATH

      - name: Generate catalog draft metadata
        shell: pwsh
        run: |
          $artifactUrl = "https://github.com/${{ github.repository }}/releases/download/$env:RELEASE_TAG/$env:PKG_NAME"
          $args = @(
            "$env:RUNNER_TEMP/hf-plugin-api/pack/dist/cli.js",
            "metadata", "$env:PKG_PATH",
            "--artifact-url", $artifactUrl,
            "--source", "${{ inputs.source }}",
            "--homepage-url", "https://haloforge.link/catalog/plugins/image-studio",
            "--repository-url", "https://github.com/${{ github.repository }}",
            "--pretty",
            "--output", "dist/plugin-release/catalog-draft.json"
          )
          if ($env:HF_PLUGIN_SIGNING_KEY_ID) {
            $args += @("--signing-key-id", $env:HF_PLUGIN_SIGNING_KEY_ID)
          }
          if ($env:HF_PLUGIN_SIGNING_PRIVATE_KEY) {
            $args += @("--signing-key-env", "HF_PLUGIN_SIGNING_PRIVATE_KEY")
          }
          node @args
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to generate catalog draft metadata."
          }

          $draft = Get-Content dist/plugin-release/catalog-draft.json -Raw | ConvertFrom-Json
          if ($draft.version.artifact_url -ne $artifactUrl) {
            throw "Generated catalog draft artifact_url does not match the release tag."
          }

      - name: Upload metadata to release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $path = "dist/plugin-release/catalog-draft.json"
          if (-not (Test-Path $path)) {
            throw "Release asset not found: $path"
          }

          for ($attempt = 1; $attempt -le 3; $attempt++) {
            & gh release upload $env:RELEASE_TAG $path --clobber
            if ($LASTEXITCODE -eq 0) {
              return
            }
            if ($attempt -eq 3) {
              throw "Failed to upload catalog-draft.json to release $env:RELEASE_TAG after $attempt attempts."
            }
            Start-Sleep -Seconds (2 * $attempt)
            & gh release view $env:RELEASE_TAG --json tagName,url *> $null
          }

      - name: Submit catalog draft
        if: env.HF_ADMIN_TOKEN != ''
        shell: pwsh
        run: node "$env:RUNNER_TEMP/hf-plugin-api/pack/dist/cli.js" submit dist/plugin-release/catalog-draft.json --api-base-url https://admin.haloforge.link --token-env HF_ADMIN_TOKEN

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugin-release-${{ github.run_id }}
          path: |
            dist/plugin-release/*.hfpkg
            dist/plugin-release/*.hfpkg.sha256
            dist/plugin-release/catalog-draft.json
